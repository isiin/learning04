server:
  port: 8080
spring:
  datasource:
    url: jdbc:potgresql://localhost:5432/testdb
    username: dbuser
    password: dbpass
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate.ddl-auto: none
